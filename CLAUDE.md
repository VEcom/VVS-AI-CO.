# CLAUDE.md — 프로젝트 작업 지침

## 답변 형식 선호 (사용자 요청, 모든 답변에 적용)

- **모든 답변 본문을 복사하기 쉽게 제공한다.** 보고/요약/설명 등 핵심 답변은
  하나의 ``` 코드블록(평문)으로 감싸서, 사용자가 한 번에 복사할 수 있게 한다.
- 코드블록 안에는 마크다운 표/볼드 대신 평문 텍스트로 정리한다(복사 시 깨지지 않도록).
- 실제 코드/명령 스니펫은 별도 코드블록으로 두어도 된다.
