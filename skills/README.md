# skills/

헤르메스 에이전트의 **스킬(능력 단위)** 을 담는다.

betplay-mvp 로직 20개가 다음 범주로 이식될 예정이다:

| 디렉터리 | 범주 | 비고 |
|----------|------|------|
| `absolute-line/` | 절대선 (헌법) | **B-1 최우선.** 다른 기능보다 먼저, 항상 강제. |
| `block/` | block 로직 | |
| `persona/` | persona 로직 | |
| `rubric/` | rubric (평가/채점) | |

> 각 하위 디렉터리는 현재 betplay 원본 로직 이식을 기다리는 골격 상태이다.
> 원본은 `betplay-mvp` 접근 또는 핸드오프 패키지 본문 제공 후 복사한다.
> (현황: `ops/B-0-STATUS.md` 참조)
