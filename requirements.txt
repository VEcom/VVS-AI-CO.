# VVS-AI-CO 런타임 의존성
#
# 절대선 강제 레이어(ai_pm/) 코어는 표준 라이브러리만 사용한다.
# 아래는 B-3 문서 렌더(PDF/PPT) 및 이미지 처리용 선택 의존성이다.
# 렌더 라이브러리 부재 시 ai_pm.document_renderer 는 RendererUnavailable 로
# 우아하게 비활성화되며, 절대선 게이트(텍스트 검증)는 영향받지 않는다.

python-pptx>=1.0,<2.0   # PPTX 렌더
reportlab>=4.0,<5.0     # PDF 렌더
Pillow>=10.0            # 썸네일/비전 검수용 이미지 처리

# 비전 디자인 검수(B-3 점수 산출)는 결정론 휴리스틱으로 동작하며,
# 실제 vision_analyze 연결은 헤르메스 tools/vision_tools.py 를 사용한다(B-4).
